name: CI Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install dependencies
        run: yarn install

      - name: Start Build
        run: yarn build

      - name: Check bundle size
        run: yarn check:size

      - name: Run Lighthouse
        run: yarn check:lighthouse
